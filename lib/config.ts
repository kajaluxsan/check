/**
 * App configuration — update these values before going live.
 *
 * CONTACT_EMAIL: where contact form submissions are delivered.
 * RESEND_API_KEY: set via environment variable RESEND_API_KEY in .env.local
 */

export const CONTACT_EMAIL = "muster@muster.ch";
